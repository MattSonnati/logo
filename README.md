# All the logos in one repo
This is a repository with logos:
- with both `coloreScheme=light` and `coloreScheme=dark` (when available)
- with both `aspectRatio=landscape`, `aspectRatio=portrait` and `aspectRatio=square` (when available)
